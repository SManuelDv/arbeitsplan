import { UNSAFE_NavigationContext as NavigationContext } from 'react-router-dom'

// Habilita os flags futuros do React Router v7
export const routerConfig = {
  future: {
    v7_relativeSplatPath: true
  }
} 